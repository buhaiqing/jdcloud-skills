# 京东云告警智能分析报告

> **本文件是报告骨架模板**。Agent 在执行 `jdcloud-alert-intelligence` 工作流 Step 5 时，
> 按本模板填充实际数据。所有 `{{...}}` 占位符须替换为真实值；无值时填「暂无相关信息」。

---

## 报告元信息

| 字段 | 值 |
|------|-----|
| **报告 ID** | `{{report_id}}`（格式：`AIR-<region>-<YYYYMMDDHHmmss>`） |
| **区域 (Region)** | `{{region}}`（如 `cn-north-1`） |
| **时间窗** | `{{start_time}}` 至 `{{end_time}}`（ISO 8601） |
| **生成时间** | `{{generated_at}}` |
| **数据源** | `jdc monitor describe-alarm-history` + `describe-alarms` |
| **数据完整性** | 原始告警总数 `{{total_raw_alarms}}` · 聚合后簇数 `{{total_clusters}}` · 抑制数 `{{suppressed_count}}` |
| **分析 Skill 版本** | `jdcloud-alert-intelligence v0.1.0` |
| **下一跳 SKILL 链** | `jdcloud-cloudmonitor-ops` / `jdcloud-vm-ops` / `jdcloud-clb-ops` / `jdcloud-redis-ops` / `jdcloud-rds-ops` |

---

## §1 执行摘要

### 1.1 关键数字

| 指标 | 数量 |
|------|------:|
| 原始告警事件总数 | `{{raw_alarm_count}}` |
| 聚合后告警簇数 | `{{cluster_count}}` |
| P0（高危）簇 | 🔴 `{{p0_count}}` |
| P1（中危）簇 | 🟡 `{{p1_count}}` |
| P2（低危）簇 | 🟢 `{{p2_count}}` |
| P3（已抑制/周期性）簇 | ⚪ `{{p3_count}}` |
| 被抑制事件数 | `{{suppressed_event_count}}` |
| 告警重复率 | `{{dup_rate}}%`（`重复事件数 / 原始告警数`） |
| 夜间打扰率 | `{{night_disturb_rate}}%`（`22:00-08:00 告警 / 原始告警`） |

### 1.2 TL;DR（一段话，不超过 80 字）

> 示例：本周期共 `{{raw_alarm_count}}` 条告警事件，聚合为 `{{cluster_count}}` 个簇；其中 `{{p0_count}}` 个 P0 需立即介入（详见 §3），主要根因集中在 `{{top_root_cause_hint}}`；降噪后实际需人工处理事件从 `{{raw_alarm_count}}` 降至 `{{human_actionable_count}}`。

---

## §2 Top 告警簇列表（按触发频次降序 · Top 10）

| # | 簇 ID | 服务 | 资源 ID | 指标 | 触发次数 | 首次触发 | 末次触发 | 持续时长 | 级别 | 状态 |
|:--:|-------|------|---------|------|---------:|---------|---------|---------:|:----:|------|
| 1 | `C-{{cluster_1_id}}` | `{{svc_1}}` | `{{res_1}}` | `{{metric_1}}` | `{{cnt_1}}` | `{{first_1}}` | `{{last_1}}` | `{{dur_1}}` | 🔴/🟡/🟢 | `{{status_1}}` |
| 2 | `C-{{cluster_2_id}}` | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| 10 | `C-{{cluster_10_id}}` | ... | ... | ... | ... | ... | ... | ... | ... | ... |

**状态枚举**：`ALARM`（持续中）/ `OK`（已恢复）/ `INSUFFICIENT_DATA`（数据不足）。

**排序规则**：触发次数降序；若相同则按持续时长降序；若再相同按首次触发时间升序。

---

## §3 P0 / P1 详单（必须人工介入）

> 每个 P0/P1 簇 1 小节。P2/P3 不进入本节，仅在 §2 与 §4 体现。

---

### 3.{{n}} 🔴 簇 `C-{{cluster_id}}` — `{{svc}}/{{res}}/{{metric}}` （P0）

**基本信息**

| 字段 | 值 |
|------|-----|
| 服务 / 资源 / 指标 | `{{service_code}}` / `{{resource_id}}` / `{{metric_name}}` |
| 告警规则 ID | `{{alarm_rule_ids}}`（可能多条规则命中同簇） |
| 触发次数 | `{{trigger_count}}` |
| 首次触发 | `{{first_trigger_at}}` |
| 末次触发 | `{{last_trigger_at}}` |
| 持续时长 | `{{duration}}`（如 `2h 17min`） |
| 峰值指标值 | `{{peak_value}}`（如 `97.3%`） |
| 触发阈值 | `{{threshold}}`（如 `>= 90%`） |
| 当前状态 | `{{current_status}}` |
| 关联事件 | `{{related_events}}`（CD 部署 / 流量峰值 / 维护窗等） |

**时间线**

```
{{timeline_text}}  示例:
10:00  首次触发 (90.2%)
10:05  升级 (95.1%)  ← 与 CD 部署完成时间一致
10:23  持续 (97.3%)  ← P0
12:17  恢复 (OK)     ← 自动恢复
```

**下一跳建议**

→ 建议使用 `jdcloud-{{next_skill}}` 执行 `{{next_action}}`（优先级 P0，{{expected_eta}}）

> 具体委托动作由用户/上层 Agent 决定；本 skill 仅给出建议，不自动执行。

---

### 示例 3.1 🔴 簇 `C-20260603-001` — vm/i-abc123/vm.cpu.util （P0）

**基本信息**

| 字段 | 值 |
|------|-----|
| 服务 / 资源 / 指标 | `vm` / `i-abc123` / `vm.cpu.util` |
| 告警规则 ID | `alarm-rule-1001`, `alarm-rule-1002` |
| 触发次数 | 17 |
| 首次触发 | 2026-06-03 10:00:12 |
| 末次触发 | 2026-06-03 12:15:33 |
| 持续时长 | 2h 15min |
| 峰值指标值 | 97.3% |
| 触发阈值 | >= 90% |
| 当前状态 | OK（已自动恢复） |
| 关联事件 | CD 部署 `cd-event-20260603-0958`（首次触发前 2min） |

**时间线**

```
10:00  首次触发 (90.2%)              ← CD 部署完成 +2min
10:05  升级 (95.1%)
10:23  持续 (97.3%)                  ← P0 升级
11:00  持续 (96.8%)
11:30  持续 (94.2%)
12:00  持续 (91.5%)
12:15  恢复 (OK 78.4%)               ← JVM warm-up 完成
```

**下一跳建议**

→ 建议使用 `jdcloud-vm-ops` 复核本次 CD 部署回滚风险（回滚版本 `v1.2.3`），并对 i-abc123 进行规格评估（CPU 平均 89% 接近瓶颈）。

---

## §4 降噪统计

### 4.1 重复率 TOP 5（按簇触发次数降序）

| 排名 | 簇 ID | 服务/资源/指标 | 触发次数 | 占原始告警比 | 降噪建议 |
|:--:|-------|----------------|---------:|------------:|----------|
| 1 | `C-{{c1}}` | `{{s1}}/{{r1}}/{{m1}}` | `{{n1}}` | `{{p1}}%` | 改告警规则（见 §5） |
| 2 | ... | ... | ... | ... | ... |
| 5 | ... | ... | ... | ... | ... |

**计算公式**：`重复率 = 1 - (聚合后簇数 / 原始告警事件数)`。本周期重复率 = `{{dup_rate}}%`。

### 4.2 夜间打扰 TOP 5（22:00 - 08:00 触发频次降序）

| 排名 | 簇 ID | 服务/资源/指标 | 夜间触发次数 | 平均触发时间 | 是否周期性 |
|:--:|-------|----------------|------------:|-------------|:----------:|
| 1 | `C-{{c1}}` | `{{s1}}/{{r1}}/{{m1}}` | `{{n1}}` | `{{avg_time_1}}` | ✅/❌ |
| 2 | ... | ... | ... | ... | ... |
| 5 | ... | ... | ... | ... | ... |

**判定周期性**：若该簇在历史 7d 同时段（±30min）触发 ≥ 3 次，则标 ✅。

**夜间打扰率**：本周期 = `{{night_disturb_rate}}%`。

### 4.3 抑制明细（按抑制原因分类）

| 抑制原因 | 簇数 | 事件数 | 占比 |
|----------|-----:|------:|-----:|
| 维护窗口 | `{{mw_clusters}}` | `{{mw_events}}` | `{{mw_pct}}%` |
| 已知周期性 | `{{period_clusters}}` | `{{period_events}}` | `{{period_pct}}%` |
| 已知误报 | `{{known_fp_clusters}}` | `{{known_fp_events}}` | `{{known_fp_pct}}%` |
| 同根因关联 | v0.1 未实现 | — | — |
| **合计** | **`{{suppressed_clusters}}`** | **`{{suppressed_events}}`** | `100%` |

**已知误报 5 类**：备份任务 / 批处理 / 滚动重启 / CD 流量回切 / 镜像拉取。

---

## §5 下一跳建议（按优先级降序）

> 建议格式：`[类型] 描述 → 委托目标`
> 类型：`[改规则]` / `[改资源]` / `[改流程]`

### 5.1 🔴 紧急建议（须 24h 内处理）

| # | 建议 | 委托目标 |
|:--:|------|----------|
| 1 | `[改资源] {{res_id}} {{metric}} 持续 P0 超 2h，建议升级规格` | → `jdcloud-vm-ops` 升级实例 |
| 2 | `[改规则] 告警规则 {{alarm_id}} 阈值 90% 在生产环境过松` | → `jdcloud-cloudmonitor-ops` 调整阈值 |
| 3 | ... | ... |

### 5.2 🟡 改进建议（本周内处理）

| # | 建议 | 委托目标 |
|:--:|------|----------|
| 1 | `[改规则] {{metric}} 在备份时段周期性触发，建议加维护窗抑制` | → `jdcloud-cloudmonitor-ops` 配置通知抑制 |
| 2 | `[改流程] {{svc}} 告警无值班升级路径，建议设置 PagerDuty 集成` | → `jdcloud-cloudmonitor-ops` 配置回调 |
| 3 | ... | ... |

### 5.3 🟢 观察建议（本月内关注）

| # | 建议 | 委托目标 |
|:--:|------|----------|
| 1 | `[改规则] 簇 {{cluster_id}} 重复率 {{dup_pct}}%，建议改为周聚合告警` | → `jdcloud-cloudmonitor-ops` 改规则粒度 |
| 2 | `[改资源] {{res_id}} 资源利用率长期 < 10%，建议降配` | → `jdcloud-vm-ops` 评估降配 |
| 3 | ... | ... |

---

## 附录 A：分析方法说明

- **聚合键**：`（service_code, resource_id, metric_name）` 三元组
- **分级依据**：见 `references/severity-matrix.md`（20 判定单元）
- **抑制规则**：见 `references/suppression-rules.md`
- **数据保留期**：原始 15d / 1h 聚合 30d / 1d 聚合 180d
- **本报告只读**：未对任何云资源或告警规则做修改

## 附录 B：术语表

| 术语 | 含义 |
|------|------|
| 告警簇 (Cluster) | 同一 `(svc, res, metric)` 三元组的多次告警聚合 |
| 重复率 | 1 − 簇数 / 原始告警数 |
| 夜间打扰率 | 22:00-08:00 触发告警 / 总告警数 |
| 周期性告警 | 历史 7d 同时段 ±30min 触发 ≥ 3 次 |
| 下一跳 | 建议委托给其他 jdcloud-*-ops 的具体动作 |

---

**报告结束 · 本文件由 `jdcloud-alert-intelligence` Skill 自动生成 · 严禁手工编辑后回传**
