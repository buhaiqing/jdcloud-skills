---
runbook_id: "04"
scenario: "大促前全链路预检"
version: "1.0.0"
trigger: "人工触发（大促前 3 天）"
risk_level: "高"
---

# 大促前全链路预检

## 1. 场景

在大促/高峰流量前，对全链路做压力前置检查，确保所有资源有余量应对 2-3 倍日常流量。

## 2. 流程

```yaml
steps:
  - id: "discover"

  - id: "baseline"
    description: "取最近 30 天业务高峰期的指标作为流量基线"
    baseline_period: "最近30天内的日间高峰（10:00-11:00, 14:00-16:00）"

  - id: "stress_assessment"
    description: "模拟 2x/3x 流量下各资源是否仍有余量"
    multiplier: 3.0
    checks:
      - "当前峰值 CPU × 3 < 80%"
      - "当前峰值 内存 × 3 < 85%"
      - "当前峰值 IOPS × 3 < 规格上限 80%"
      - "当前峰值 带宽 × 3 < 规格上限 80%"
      - "当前峰值 Redis 内存 × 3 < 85%"
      - "当前峰值 CLB 并发 × 3 < 规格上限 80%"
      - "当前峰值 NAT SNAT × 3 < 规格上限 80%"
    outputs:
      - "余量充足的资源 ✅"
      - "需要升配的资源 ⚠️"
      - "建议升配规格"

  - id: "report"
    outputs:
      markdown: true
      format: "压力评估式（基线值 × 倍数 → 是否达标 → 建议升配规格）"
```

## 3. 示例输出

```
═══════════════════════════════════════
  大促前预检报告
═══════════════════════════════════════
  客户: 烟台振华 │ 预估流量: 3x 日常峰值
═══════════════════════════════════════

✅ 余量充足
  VM CPU:    峰值25% × 3 = 75% < 80% → 合格
  磁盘IOPS:  峰值3115 × 3 = 9345 < 50200 → 合格

⚠️ 建议升配
  CLB 并发:  峰值3500 × 3 = 10500 > 规格上限8000 → 建议升配CLB规格
  NAT SNAT: 峰值6000 × 3 = 18000 > 规格上限15000 → 建议升配NAT规格
```